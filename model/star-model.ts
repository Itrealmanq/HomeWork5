// สร้าง interface ชื่อ StarsGet ซึ่งกำหนดรูปแบบของข้อมูลที่จะใช้ในการรับข้อมูลดาว
export interface StarsGet {
    moviename: string;    // ชื่อของหนัง (ชนิดข้อมูลเป็น string)
    personname: string;   // ชื่อของบุคคล (ชนิดข้อมูลเป็น string)
}
